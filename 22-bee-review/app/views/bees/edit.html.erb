<%= form_for @bee do |f| %>
  <%= f.text_field :name %>
  <%= f.label "Is queen?" %>
  <%= f.check_box :queen %>
  <%= f.collection_select :hive_id, Hive.all, :id, :name %>
  <%= f.submit %>
<% end %>
